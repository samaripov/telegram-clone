<%= turbo_frame_tag "main" do %>
  <h1><%= @user.username %>'s DM</h1>
  <%= button_to "Send", chats_path(user_ids: [current_user.id, @user.id]), method: :post%>
  <%= render "chats/chat_form", user_ids: [current_user.id, @user.id]%>
<% end %>