<%= turbo_frame_tag "main" do %>
  <%= turbo_stream_from "#{current_user.id}-chats" %>
  <div id="users-chats">
    <%= render partial: "chats/chat", collection: @open_chats, as: :chat %>
  </div>
<% end %>